<?php

return [
    'otp' => [
        /*
        |--------------------------------------------------------------------------
        | OTP Expiration
        |--------------------------------------------------------------------------
        |
        | عدد الدقائق التي يبقى خلالها كود التحقق صالحًا.
        |
        */
        'expires_minutes' => (int) env(
            'OTP_EXPIRES_MINUTES',
            10
        ),

        /*
        |--------------------------------------------------------------------------
        | Maximum Verification Attempts
        |--------------------------------------------------------------------------
        |
        | عدد المحاولات الخاطئة المسموحة قبل إبطال الكود.
        |
        */
        'max_attempts' => (int) env(
            'OTP_MAX_ATTEMPTS',
            5
        ),

        /*
        |--------------------------------------------------------------------------
        | Resend Rate Limit
        |--------------------------------------------------------------------------
        |
        | عدد طلبات إعادة الإرسال المسموحة خلال فترة زمنية محددة.
        |
        */
        'resend_max_attempts' => (int) env(
            'OTP_RESEND_MAX_ATTEMPTS',
            3
        ),

        /*
        |--------------------------------------------------------------------------
        | Resend Decay
        |--------------------------------------------------------------------------
        |
        | المدة بالثواني التي نحسب خلالها محاولات إعادة الإرسال.
        |
        */
        'resend_decay_seconds' => (int) env(
            'OTP_RESEND_DECAY_SECONDS',
            60
        ),
    ],
];
